import {css, Theme} from "@emotion/react";

const styles = {
    wrap: css({
        display: "flex",
    }),
    formContainer: css({
        flex: 2
    }),
    right: css({
        flex: 2
    }),
};

export default styles;
