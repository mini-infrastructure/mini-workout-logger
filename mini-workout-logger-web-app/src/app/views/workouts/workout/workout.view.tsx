import Layout from "../../../components/layout/layout.component.tsx";

const WorkoutView = () => {
    return (
        <Layout>
            <h2>Workout</h2>
        </Layout>
    );
};

export default WorkoutView;
