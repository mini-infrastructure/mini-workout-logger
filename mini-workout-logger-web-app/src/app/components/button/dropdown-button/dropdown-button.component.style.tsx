import {css} from '@emotion/react';

const styles = {
    container: css({
        position: 'relative',
    }),

    triggerActive: css({
        border: `var(--borderWidth-thin) solid var(--color-blue)`,
    }),
};

export default styles;
